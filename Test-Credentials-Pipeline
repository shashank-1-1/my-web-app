pipeline {
  agent any
  stages {
    stage('Test Credentials') {
      steps {
        echo "OpenShift Server: ${credentials('my-openshift-server')}"
        echo "OpenShift Token: ${credentials('my-openshift-token')}"
        echo "Docker Username: ${credentials('docker-username')}" 
      }
    }
  }
}
